import 'package:flutter/material.dart';

/// Clase que define los colores personalizados de la aplicación
class TravelColors {
  /// Color principal de la aplicación
  static const Color primary = Color(0xFF1565C0);

  /// Color secundario de la aplicación
  static const Color secondary = Color(0xFF26A69A);

  /// Color de acento para elementos importantes
  static const Color accent = Color(0xFFFF9800);

  /// Color para mensajes de información
  static const Color info = Color(0xFF2196F3);

  /// Color para mensajes de éxito
  static const Color success = Color(0xFF4CAF50);

  /// Color para mensajes de advertencia
  static const Color warning = Color(0xFFFFC107);

  /// Color para mensajes de error
  static const Color error = Color(0xFFF44336);

  /// Color de fondo principal
  static const Color background = Color(0xFFF5F5F5);

  /// Color de superficie (tarjetas, diálogos)
  static const Color surface = Colors.white;

  /// Color para elementos en superficie en modo oscuro
  static const Color onDarkSurface = Colors.white;

  /// Color para dividers y separadores
  static const Color divider = Color(0xFFE0E0E0);

  /// Color de texto principal
  static const Color textPrimary = Color(0xFF212121);

  /// Color de texto secundario
  static const Color textSecondary = Color(0xFF757575);

  /// Color para elementos deshabilitados
  static const Color disabled = Color(0xFFBDBDBD);

  /// Color para bordes y divisores
  static const Color border = Color(0xFFE0E0E0);

  /// Color para elementos superimpuestos en la superficie de fondo
  static const Color onSurface = Color(0xFF212121);

  /// Color para elementos primarios (botones, etc.)
  static const Color onPrimary = Colors.white;

  /// Color para elementos de acento
  static const Color onSecondary = Colors.white;
}
